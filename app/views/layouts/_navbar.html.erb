<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <%= link_to "Kurt's Blog", root_path, class: "navbar-brand", id: "logo" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <%= link_to "Articles", articles_path, class: "nav-link" %>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false">
                    Actions
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a><%= link_to "New Article", new_article_path, class: "dropdown-item" %></a>
                </div>
            </li>
        </ul>

        <% if logged_in? %>
            <a class="nav-link" href="#"><%= link_to 'Log Out', logout_path, method: :delete %> </a>
        <% else%>
            <a href="#"><%= link_to 'Log In', login_path,  class: "nav-link"  %> </a>
            <a href="#"><%= link_to 'Sign Up', signup_path,  class: "nav-link"  %> </a>
        <% end %>
    </div>
</nav>