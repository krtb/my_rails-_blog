<% if obj.errors.any? %>
<div class="container">
    <div class="row">
        <div class="card border-danger mb-3 col-sm-4 offset-sm-4">
            <div class="card-body text-danger">
                <h5 class="card-title">
                    <%= pluralize(obj.errors.count, "error" ) %>
                    prohibited this article from being saved.
                </h5>
                <div class="card-text">
                    <ul>
                        <% obj.errors.full_messages.each do |msg| %>
                        <li>
                            <%= msg %>
                        </li>
                        <% end %>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
<% end %>